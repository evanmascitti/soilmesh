below_vol <- function(mesh){

  # slice the mesh along the x-y plane and keep only what's beneath
  # use the rounded circle (which I'm in the process of creating in inst/extdata
  # and then saving as a .rda file)

}
